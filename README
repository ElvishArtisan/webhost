This is the README file for the webhost package.

Webhost is a library for embedding HTML interfaces into programs. It
includes two basic parts: a framework for generating and processing
configuration web pages, and an integrated websockets-capable web server.
It uses the Qt4 toolkit.
